export type REGuserData = {
    login: string;
    userName: string;
    password: string;
};
export type AUTuserData = {
    login: string;
    password: string;
};

export type Ttokens = {
    AccessToken: string;
    RefreshToken: string;
};